appId: com.kathy178.desktopcalendarwidget
productName: 桌面悬浮日历
# 只在本地/CI 生成安装包文件，不发布到 GitHub Releases（不需要配置 GH_TOKEN）
publish: null
directories:
  output: release
  buildResources: build
files:
  - out/**/*
  - package.json
asarUnpack:
  - resources/**
win:
  target:
    - target: nsis
      arch: [x64]
    - target: portable
      arch: [x64]
  artifactName: ${productName}-${version}-${arch}.${ext}
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: 桌面悬浮日历
extraMetadata:
  main: out/main/index.js
